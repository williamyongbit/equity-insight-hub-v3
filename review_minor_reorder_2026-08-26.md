# Minor Adjustment Review — 26 August 2026

## Desktop local route

The local English U.S. route was opened at 1280×900 after the requested ordering adjustment. It loaded with HTTP 304, zero application console errors and one non-blocking warning. A visual capture and section-order inspection follow to confirm that the company/industry evidence appears before the dark sector heatmap, while the 1D/5D/20D controls remain available.

The full desktop screenshot confirms the intended reading order: completed session, macro/market drivers, Stocks / Industry Chain cards, then the dark Sector Heatmap. The heatmap retains its 1D/5D/20D controls, green/grey/orange-red performance semantics and complete 11-sector data grid. No clipping or horizontal overflow was observed in the desktop capture.

## Narrow-screen local route

The same local U.S. route was resized to 390×844 and a full-page screenshot was captured. Visual inspection follows to confirm the mobile content order, control visibility and lack of horizontal overflow.

The 390px screenshot confirms the same order as desktop: the three Stocks / Industry Chain cards are fully readable before the dark heatmap. The language selector, complete-PDF action and 1D/5D/20D controls remain visible. The heatmap stacks into a single readable column, with no page-level horizontal overflow observed.

## Full-report PDF route

The local Traditional Chinese `?date=2026-08-26&lang=TW&export=pdf` route reloaded with HTTP 304, zero application console errors and one non-blocking warning. A print-route snapshot will confirm that the U.S. section retains the cards-before-heatmap order inside the six-section document.

The full PDF-route accessibility snapshot was captured with zero application console errors and one non-blocking warning. It is retained as the print-layout evidence for the reordered U.S. section.

The PDF snapshot confirms the print sequence in Traditional Chinese: completed session, macro/market drivers, 個股／產業鏈 cards, then the sector heatmap grid. The local English U.S. reader was subsequently reopened without application errors to test the retained 1D/5D/20D controls.

The 5D window button was activated successfully after reordering. The local U.S. page retained its URL and produced zero application console errors with one non-blocking warning; a post-click snapshot was captured to verify the 5D values and active state.
