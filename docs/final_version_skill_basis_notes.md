## Basis captured from 24–25 Aug final product and legacy prompt

- The old prompt in `pasted_content_16.txt` captures the core research discipline correctly: dynamic Asia/Hong_Kong date handling; completed U.S. and Hong Kong session boundaries; morning-snapshot Hong Kong intraday labeling; user-material-first reading; verification hierarchy; Yesterday → Today continuity; and Macro → Stocks → Sector logic.
- The 24–25 Aug final product adds important implementation details that are not fully encoded in the old prompt and therefore must be added to the reusable skill.
- Final V3 product features that must be encoded in the new skill include: BIT white/black/blue brand shell; fixed latest-report root URL plus visible archive/edition registry; query-driven dated historical access; full TW/CN/EN localization; genuinely distinct Education versus Analysis reading modes; six public reader paths (Overview, U.S., Hong Kong, Cross-market, This Week, Sources); complete-report browser-native PDF export; standalone HTML packaging option; responsive controls that remain visible on narrow devices; and company-maintenance hosting guidance.
- Final 25 Aug content/product lessons that must enter the skill: remove personal “William’s View” framing for internal releases; keep internal-use research-only disclosure; separate completed-session facts from intraday snapshots and from scheduled future events; keep Hong Kong dual-clock handling explicit; use a data-driven daily archive registry rather than hardcoded version cards; preserve 1D/5D/20D heatmap window controls while removing the old SVG export; keep green/grey/orange-red performance semantics; and remove outdated explanatory sentences that no longer match the UI.
- PDF pages reviewed confirm the final printable experience: the export route is a full-document view that serializes the six sections into one print/PDF document with BIT header, as-of line, section dividers and no interactive drawer chrome.
- The skill should not merely contain editorial prompt text. It should capture the reusable operating system: source-ingestion sequence, structured-data expectations, history/archive rules, validation gates, browser checks, responsive checks, deployment/hosting decisions, and fallback behavior when sources or Hong Kong close data are unavailable.
