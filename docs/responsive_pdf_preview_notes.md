# Responsive PDF / Archive Revision — Local Preview Notes

## iPhone-sized preview — 390 × 844

The revised local U.S. page retains the BIT app mark and wordmark, all three language controls (TW, CN, EN), the complete-PDF export control, six research-path navigation labels and the edition archive trigger in the visible viewport. The report starts with consistent horizontal padding and no visible left-edge clipping or horizontal-page offset. The former 1D / 5D / 20D and SVG export controls are absent from the reviewed path.

The PDF action opens a dedicated all-sections print route. The native browser print dialog is intentionally used for final PDF creation because it preserves browser-supported CJK fonts and lets Windows and iPhone users save a genuine PDF without a client-side rasterisation dependency.

## Windows-laptop preview — 1365 × 768

The normal Simplified Chinese overview is horizontally centered within the report shell. The header retains the TW, CN and EN controls plus the complete-PDF action at the right edge. The report remains readable without the prior whole-page left offset, while the edition archive stays available as a non-print floating action. The retired 1D / 5D / 20D and SVG export controls are absent.
