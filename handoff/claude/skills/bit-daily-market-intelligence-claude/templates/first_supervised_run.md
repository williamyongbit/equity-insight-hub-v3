# First Supervised Run

Use the first-supervised-run prompt in the repository handoff. Require research, a full draft and validation evidence, then stop before `git push` or deployment for human review.
